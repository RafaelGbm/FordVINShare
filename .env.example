EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1

# Set to "true" to skip login and populate the React Query cache with
# realistic fake data so the app can be demonstrated without a live
# Java backend. Leave empty (or "false") in real environments.
EXPO_PUBLIC_DEMO_MODE=false
